mixin formProject( project = {} )

    //- Mixin (Pug): Permite que el mismo formulario se use para crear nuevo proyecto y editar proyecto
    form.agregar-proyecto( action=`${ project.id ? '/project/save/' + project.id : '/project/new' }` method="post" ) 
        .campo
            label( for="project_name" ) Nombre proyecto:
            input( value=project.name type="text" id="nombre" name="project_name" placeholder="Nombre del proyecto" )
        .campo.enviar
            input(type="submit" value=`${ project.name ? 'Guardar' : 'Agregar' }` class="boton")
        