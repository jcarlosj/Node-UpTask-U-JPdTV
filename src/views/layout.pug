doctype html
html(lang="en")
    head
        include partials/head
    body 
        header.barra
            h1
                a(href="/") UpTask - Administrador de proyectos
            a(href="#") Cerrar sesión
            
        main.contenedor
            aside.contenedor-proyectos
                .panel.crear-proyecto 
                    a(href="/project/new" class="boton") Nuevo proyecto
                        span +
                .panel.lista-proyectos
                    h2 Proyectos
                    ul#proyectos.proyectos 
                        if ( projects.length )
                            each project in projects
                                li
                                    a(href=`/project/${ project.url }`) #{ project.name }
            block main_content
            //- pre #{ vardump( projects ) }
        script( src="/dist/bundle.js" )
        script( src="/dist/svg.js" )